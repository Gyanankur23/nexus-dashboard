name: nexus_dashboard_backend
description: Backend API for Nexus Dashboard - Admin & Project Management
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.1
  shelf_router: ^1.1.4
  shelf_cors: ^0.2.0
  crypto: ^3.0.3
  dart_jsonwebtoken: ^2.12.0

dev_dependencies:
  lints: ^3.0.0
